# Requirements for production-ready utilities
# This file contains dependencies for advanced logging, secrets management,
# security auditing, and DVC integration

# Core utilities
pyyaml>=6.0
python-dotenv>=1.0.0
click>=8.0.0
rich>=13.0.0
tqdm>=4.64.0

# Logging and monitoring
structlog>=23.0.0
python-json-logger>=2.0.0
colorlog>=6.7.0
prometheus-client>=0.16.0

# Security and secrets management
cryptography>=41.0.0
keyring>=24.0.0
hvac>=1.2.0  # HashiCorp Vault client
boto3>=1.28.0  # AWS SDK
azure-keyvault-secrets>=4.7.0
azure-identity>=1.13.0
google-cloud-secret-manager>=2.16.0

# Security scanning tools
bandit>=1.7.5
safety>=3.0.0
# Note: Trivy is installed via shell script, not pip

# DVC and experiment tracking
dvc>=3.0.0
dvc[s3]>=3.0.0  # S3 support
dvc[azure]>=3.0.0  # Azure support
dvc[gs]>=3.0.0  # Google Cloud support
dvc[ssh]>=3.0.0  # SSH support
mlflow>=2.7.0
wandb>=0.15.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=13.0.0

# Network and HTTP
requests>=2.31.0
httpx>=0.24.0
aiohttp>=3.8.0

# Async support
asyncio-mqtt>=0.13.0
aiofiles>=23.0.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
coverage>=7.3.0

# Optional: Database support
sqlalchemy>=2.0.0
alembic>=1.12.0
redis>=4.6.0

# Optional: Message queues
celery>=5.3.0
kombu>=5.3.0
